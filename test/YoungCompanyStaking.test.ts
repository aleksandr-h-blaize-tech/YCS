import { expect } from "chai";
import { ethers } from "hardhat";

describe("Contract: YoungConpanyStaking", () => {
    it("Should return the new greeting once it's changed", async () => {
        
    });
});
